# Example Data

These CSV files provide small, reproducible inputs for local checks and demos.

Graph Lab files use:

```csv
src,dst,weight,confidence
```

`confidence` is expected on a 0..100 scale.

AB Lab files use event-level rows with group, conversion and revenue fields. They are included as generic sample inputs for projects that consume A/B experiment events.
