# Example Data

These CSV files provide small, reproducible inputs for local checks and demos.

Graph Lab files use:

```csv
src,dst,weight,confidence
```

`confidence` is expected on a 0..100 scale.
